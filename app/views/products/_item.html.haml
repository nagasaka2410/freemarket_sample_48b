= link_to product_path(item) ,class: "item-boxes" do
  .item-box
    .item-box__photo
      = image_tag(item.images.first.name)
    .item-box__body
      .item-box__body--text
        %p
          = item.name
      .item-box__body--block
        .product-price
          %p ¥#{item.price}
        .product-evaluation
          %i.far.fa-heart.product-evaluation__heart-icon
          .product-evaluation__count
            %p 5